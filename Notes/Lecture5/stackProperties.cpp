/** @file: stackProperties.cpp */

// Generally, the member functions of a stack include:

//  1.) create a new stack
//  2.) push: put an item onto a stack
//  3.) pop: remove an item off the stack
//  4.) top: look at the top item of the stack
//  5.) check if it is empty
//  6.) how many items are on the stack

// use the header #include <stack>

/*
#include <stack>
#include <iostream>
using namespace std;

int main()
{
    stack<int> s;
    s.push(10);
    s.push(20);
    int k = s.top(); // has the value 20
    cout << k << endl;
    s.pop();
    if (!s.empty())
        cout << s.size() << endl;
 
} // end main
*/

