/** @file: main.cpp */

#include <iostream>
using namespace std;

int main()
{
    // TYPES OF ARRAYS:
    
    // FIXED ARRAY SIZE:
    int x[10];
    
    // DYNAMICALLY ALLOCATED ARRAY:
    int* iPtr;
    int size;
    iPtr = new int[size];
    
    // RESIZEABLE ARRAY:
    /** if (item count > the size of the array)
            dynamically allocate a new array that is larger
            copy the array into the new array */
    

    // LINKED LISTS:
}